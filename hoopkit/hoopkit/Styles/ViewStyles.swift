import SwiftUI

struct ViewStyles {
    // 字体
    static let labelFont = Font.system(size: 14)
    static let smallFont = Font.system(size: 13)
    
    // 颜色
    static let labelColor = Color.secondary
    static let backgroundColor = Color(red: 0.98, green: 0.96, blue: 0.94)
    
    // 间距
    static let defaultPadding: CGFloat = 12
    static let smallPadding: CGFloat = 8
    static let tinyPadding: CGFloat = 4
    
    // 圆角
    static let cornerRadius: CGFloat = 8
    
    // 组件高度
    static let buttonHeight: CGFloat = 32
} 